#include "sorting.h"



int main(void){
    
    /*****************TESTE*********************
    
    List *arr;
    buildList(arr);
    
    int i, j;
    tpElem x;
    for(i = 0; i < 5; i++)
    {
        scanf("%d", &x);
        j = insert(arr, x);
        if(j == Error)
        {    
            printf("Error!\n");
            break;
        }    
    }
    HeapSort(arr);
    printList(arr);
   ****************************************************/
    return 0;
}